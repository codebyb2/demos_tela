<!DOCTYPE html>
<html lang="pt-BR">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>C Law 2025 - Erro</title>
    
    <!-- Fonts -->
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700;800&display=swap" rel="stylesheet">
    
    <style>
        :root {
            --primary-black: #000000;
            --primary-blue: #0066FF;
            --text-dark: #1a1a1a;
            --text-gray: #666666;
            --text-light: #999999;
            --background: #ffffff;
            --surface: #f8f9fa;
            --border: #e5e5e5;
            --error: #EF4444;
        }

        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            font-family: 'Inter', -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, sans-serif;
            background: var(--background);
            color: var(--text-dark);
            line-height: 1.6;
            min-height: 100vh;
            padding: 1rem;
            display: flex;
            align-items: center;
            justify-content: center;
        }

        .container {
            max-width: 450px;
            width: 100%;
            text-align: center;
            padding: 1.5rem 1rem;
        }

        /* Logo */
        .logo-section {
            margin-bottom: 1.5rem;
        }

        .logo-img {
            height: clamp(28px, 5.5vw, 42px);
            width: auto;
        }

        /* Error Icon */
        .error-icon {
            font-size: 3rem;
            margin-bottom: 1rem;
            display: block;
        }

        /* Error Title */
        .error-title {
            font-size: clamp(1.5rem, 4vw, 1.75rem);
            font-weight: 700;
            color: var(--text-dark);
            margin-bottom: 0.75rem;
            line-height: 1.3;
        }

        /* Error Message */
        .error-message {
            font-size: clamp(0.875rem, 2.5vw, 1rem);
            color: var(--text-gray);
            margin-bottom: 2rem;
            line-height: 1.6;
            max-width: 360px;
            margin-left: auto;
            margin-right: auto;
        }

        /* Buttons */
        .button-group {
            display: flex;
            flex-direction: column;
            gap: 0.75rem;
            align-items: center;
            max-width: 280px;
            margin: 0 auto;
            margin-bottom: 1.5rem;
        }

        .btn {
            display: inline-flex;
            align-items: center;
            justify-content: center;
            padding: 0.875rem 1.5rem;
            border-radius: 8px;
            font-size: 0.9rem;
            font-weight: 600;
            text-decoration: none;
            cursor: pointer;
            transition: all 0.2s ease;
            width: 100%;
            border: 2px solid transparent;
            min-height: 48px;
        }

        .btn:hover {
            transform: translateY(-2px);
            box-shadow: 0 8px 16px -4px rgb(0 0 0 / 0.2);
        }

        .btn-primary {
            background: var(--primary-black);
            color: white;
            border-color: var(--primary-black);
        }

        .btn-primary:hover {
            background: #333333;
            border-color: #333333;
            color: white;
        }

        .btn-secondary {
            background: transparent;
            color: var(--text-dark);
            border-color: var(--border);
        }

        .btn-secondary:hover {
            background: var(--surface);
            color: var(--text-dark);
            border-color: var(--text-gray);
        }

        /* Footer */
        .footer {
            font-size: 0.875rem;
            color: var(--text-light);
            margin-top: 2rem;
        }

        /* Animations */
        @keyframes fadeIn {
            from { 
                opacity: 0; 
                transform: translateY(20px); 
            }
            to { 
                opacity: 1; 
                transform: translateY(0); 
            }
        }

        .fade-in {
            animation: fadeIn 0.6s ease-out;
        }

        /* Responsive */
        @media (max-width: 768px) {
            body {
                padding: 0.5rem;
                align-items: flex-start;
                padding-top: 10vh;
            }
            
            .container {
                padding: 1rem 0.5rem;
            }

            .logo-section {
                margin-bottom: 1.5rem;
            }

            .error-icon {
                font-size: 3rem;
                margin-bottom: 1rem;
            }

            .error-message {
                margin-bottom: 2rem;
            }

            .button-group {
                max-width: 100%;
                gap: 0.75rem;
            }
        }

        @media (max-width: 480px) {
            body {
                padding-top: 5vh;
            }

            .container {
                padding: 0.5rem;
            }

            .logo-img {
                height: 40px;
            }

            .logo-section {
                margin-bottom: 1rem;
            }

            .btn {
                padding: 0.875rem 1.5rem;
                font-size: 0.95rem;
            }
        }

        @media (min-width: 769px) {
            .button-group {
                flex-direction: row;
                justify-content: center;
                max-width: 400px;
            }

            .btn {
                min-width: 140px;
                max-width: 180px;
            }
        }
    </style>
</head>
<body>
    <div class="container fade-in">
        <!-- Logo -->
        <div class="logo-section">
            <img src="https://www.tela.com/_vercel/image?url=%2Ftela-logo.png&w=320&q=100" alt="Tela Logo" class="logo-img">
        </div>
        
        <!-- Error Content -->
        <div class="error-content">
            <h1 class="error-title">
                Ops! Algo deu errado
            </h1>
            
            <p class="error-message">
                <%= typeof message !== 'undefined' ? message : 'Ocorreu um erro inesperado. Nossa equipe foi notificada.' %>
            </p>
        </div>
        
        <!-- Buttons -->
        <div class="button-group">
            <a href="javascript:history.back()" class="btn btn-secondary">
                Voltar
            </a>
            
            <a href="/" class="btn btn-primary">
                Início
            </a>
        </div>
        
        <!-- Footer -->
        <div class="footer">
            Se o problema persistir, entre em contato com nossa equipe.<br>
            &copy; 2025 Tela. Todos os direitos reservados.
        </div>
    </div>

    <script>
        // Animação de entrada
        document.addEventListener('DOMContentLoaded', function() {
            document.querySelector('.container').classList.add('fade-in');
        });
    </script>
</body>
</html> 