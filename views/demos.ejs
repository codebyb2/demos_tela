<!DOCTYPE html>
<html lang="pt-BR">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>C Law 2025 - Demonstrações Tela</title>
    
    <!-- Fonts -->
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700;800&display=swap" rel="stylesheet">
    
    <!-- Meta tags -->
    <meta name="description" content="Demonstrações exclusivas Tela para C-Law Experience 2025">
    <meta name="robots" content="noindex, nofollow">
    
    <style>
        :root {
            --primary-black: #000000;
            --primary-blue: #0066FF;
            --text-dark: #1a1a1a;
            --text-gray: #666666;
            --text-light: #999999;
            --background: #ffffff;
            --surface: #f8f9fa;
            --border: #e5e5e5;
            --success: #10B981;
        }

        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            font-family: 'Inter', -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, sans-serif;
            background: var(--background);
            color: var(--text-dark);
            line-height: 1.6;
            min-height: 100vh;
            padding: 1rem;
        }

        .container {
            max-width: 1000px;
            margin: 0 auto;
            padding: 2rem 1rem;
            width: 100%;
        }

        /* Header */
        .header {
            text-align: center;
            margin-bottom: 3rem;
        }

        .logo-img {
            height: clamp(40px, 8vw, 60px);
            width: auto;
            margin-bottom: 2rem;
        }

        .welcome-title {
            font-size: clamp(2rem, 5vw, 2.75rem);
            font-weight: 600;
            color: var(--text-dark);
            margin-bottom: 1rem;
            line-height: 1.2;
        }

        .welcome-subtitle {
            font-size: clamp(1rem, 3vw, 1.25rem);
            color: var(--text-gray);
            margin-bottom: 1rem;
        }

        .user-info {
            background: var(--surface);
            padding: 1rem;
            border-radius: 12px;
            margin: 2rem auto;
            max-width: 400px;
            border: 1px solid var(--border);
        }

        .user-info h3 {
            font-size: 1rem;
            font-weight: 600;
            color: var(--text-dark);
            margin-bottom: 0.5rem;
        }

        .user-info p {
            font-size: 0.875rem;
            color: var(--text-gray);
        }

        /* Demos Grid */
        .demos-grid {
            display: grid;
            grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
            gap: 2rem;
            margin-bottom: 3rem;
        }

        .demo-card {
            background: var(--background);
            border: 1px solid var(--border);
            border-radius: 16px;
            padding: 2rem;
            text-align: center;
            transition: all 0.3s ease;
            box-shadow: 0 2px 4px -1px rgb(0 0 0 / 0.1);
            display: flex;
            flex-direction: column;
            height: 100%;
        }

        .demo-card:hover {
            transform: translateY(-4px);
            box-shadow: 0 12px 24px -8px rgb(0 0 0 / 0.15);
            border-color: var(--primary-black);
        }

        .demo-title {
            font-size: 1.5rem;
            font-weight: 600;
            color: var(--text-dark);
            margin-bottom: 1rem;
        }

        .demo-description {
            font-size: 1rem;
            color: var(--text-gray);
            margin-bottom: 2rem;
            line-height: 1.6;
            flex-grow: 1;
        }

        .btn {
            display: inline-flex;
            align-items: center;
            justify-content: center;
            padding: 1rem 2rem;
            border-radius: 12px;
            font-size: 1rem;
            font-weight: 600;
            text-decoration: none;
            cursor: pointer;
            transition: all 0.2s ease;
            width: 100%;
            border: 2px solid transparent;
            min-height: 54px;
        }

        .btn:hover {
            transform: translateY(-2px);
            box-shadow: 0 8px 16px -4px rgb(0 0 0 / 0.2);
        }

        .btn-primary {
            background: var(--primary-black);
            color: white;
            border-color: var(--primary-black);
        }

        .btn-primary:hover {
            background: #333333;
            border-color: #333333;
            color: white;
        }

        .btn-secondary {
            background: transparent;
            color: var(--text-dark);
            border-color: var(--border);
        }

        .btn-secondary:hover {
            background: var(--surface);
            color: var(--text-dark);
            border-color: var(--text-gray);
        }

        /* Footer Actions */
        .footer-actions {
            text-align: center;
            margin-top: 3rem;
            padding-top: 2rem;
            border-top: 1px solid var(--border);
        }

        /* Animations */
        @keyframes fadeIn {
            from { 
                opacity: 0; 
                transform: translateY(20px); 
            }
            to { 
                opacity: 1; 
                transform: translateY(0); 
            }
        }

        .fade-in {
            animation: fadeIn 0.6s ease-out;
        }

        /* Responsive */
        @media (max-width: 768px) {
            .demos-grid {
                grid-template-columns: 1fr;
                gap: 1.5rem;
            }

            .demo-card {
                padding: 1.5rem;
            }
        }
    </style>
</head>
<body>
    <div class="container">
        <!-- Header -->
        <div class="header fade-in">
            <img src="<%= logoUrl %>" alt="Tela Logo" class="logo-img">
            
            <h1 class="welcome-title">
                Bem-vindo às Demonstrações Tela
            </h1>
            
            <p class="welcome-subtitle">
                C-Law Experience 2025
            </p>

            <% if (typeof userProfile !== 'undefined' && userProfile) { %>
                <div class="user-info">
                    <h3>👋 Olá, <%= userProfile.name %>!</h3>
                    <p>Autenticado em <%= new Date(authenticatedAt).toLocaleString('pt-BR') %></p>
                </div>
            <% } %>
        </div>

        <!-- Demos Grid -->
        <div class="demos-grid">
            <% Object.keys(demos).forEach(function(demoId) { %>
                <div class="demo-card fade-in">
                    <h2 class="demo-title">
                        <%= demos[demoId].title %>
                    </h2>
                    
                    <p class="demo-description">
                        Demonstração exclusiva da funcionalidade <%= demos[demoId].title.toLowerCase() %> do Tela.
                    </p>
                    
                    <a href="/watch?demo=<%= encodeURIComponent(demoId) %>" class="btn btn-primary">
                        Assistir
                    </a>
                </div>
            <% }); %>
        </div>

        <!-- Footer Actions -->
        <div class="footer-actions">
            <a href="/logout" class="btn btn-secondary" style="max-width: 200px;">
                🚪 Sair
            </a>
        </div>
    </div>

    <script>
        // Add staggered animation delays
        document.addEventListener('DOMContentLoaded', function() {
            const elements = document.querySelectorAll('.fade-in');
            elements.forEach((element, index) => {
                element.style.animationDelay = `${index * 0.1}s`;
            });
        });
    </script>
</body>
</html> 