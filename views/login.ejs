<!DOCTYPE html>
<html lang="pt-BR">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>C Law 2025 - Demo <%= demo.title %></title>
    
    <!-- Fonts -->
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700;800&display=swap" rel="stylesheet">
    
    <!-- Meta tags -->
    <meta name="description" content="<%= demo.title %> - Demonstração exclusiva Tela para C-Law Experience 2025">
    <meta name="robots" content="noindex, nofollow">
    
    <style>
        :root {
            --primary-black: #000000;
            --primary-blue: #0066FF;
            --text-dark: #1a1a1a;
            --text-gray: #666666;
            --text-light: #999999;
            --background: #ffffff;
            --surface: #f8f9fa;
            --border: #e5e5e5;
        }

        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            font-family: 'Inter', -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, sans-serif;
            background: var(--background);
            color: var(--text-dark);
            line-height: 1.6;
            min-height: 100vh;
            padding: 1rem;
            display: flex;
            align-items: center;
            justify-content: center;
        }

        .container {
            max-width: 600px;
            width: 100%;
            text-align: center;
            padding: 2rem 1rem;
        }

        /* Logo */
        .logo-section {
            margin-bottom: 2rem;
        }

        .logo-img {
            height: clamp(40px, 8vw, 60px);
            width: auto;
        }

        /* Demo Title */
        .demo-title {
            font-size: clamp(2rem, 5vw, 3rem);
            font-weight: 600;
            color: var(--text-dark);
            margin-bottom: 1rem;
            line-height: 1.1;
            white-space: nowrap;
        }

        /* Subtitle */
        .demo-subtitle {
            font-size: clamp(1rem, 3vw, 1.25rem);
            color: var(--text-gray);
            margin-bottom: 2.5rem;
            font-weight: 400;
            max-width: 500px;
            margin-left: auto;
            margin-right: auto;
        }

        /* LinkedIn Button */
        .btn-linkedin {
            display: inline-flex;
            align-items: center;
            justify-content: center;
            padding: 1rem 2rem;
            background: var(--primary-black);
            color: white;
            border: none;
            border-radius: 12px;
            font-size: 1rem;
            font-weight: 600;
            text-decoration: none;
            cursor: pointer;
            transition: all 0.2s ease;
            width: 100%;
            max-width: 280px;
            margin-bottom: 2rem;
            border: 2px solid var(--primary-black);
            min-height: 54px;
        }

        .btn-linkedin:hover {
            background: #333333;
            border-color: #333333;
            transform: translateY(-2px);
            box-shadow: 0 8px 16px -4px rgb(0 0 0 / 0.2);
            color: white;
        }

        /* Privacy Info */
        .privacy-info {
            background: var(--surface);
            border-radius: 12px;
            padding: 1.5rem;
            margin-bottom: 1.5rem;
            text-align: left;
            border: 1px solid var(--border);
        }

        .privacy-info h4 {
            font-size: 0.875rem;
            font-weight: 600;
            color: var(--text-dark);
            margin-bottom: 0.75rem;
            display: flex;
            align-items: center;
        }

        .privacy-info h4::before {
            content: "🔒";
            margin-right: 0.5rem;
        }

        .privacy-compact {
            font-size: 0.75rem;
            color: var(--text-gray);
            line-height: 1.4;
        }

        .privacy-compliance {
            font-size: 0.75rem;
            color: var(--text-gray);
            margin-top: 0.75rem;
            padding-top: 0.75rem;
            border-top: 1px solid var(--border);
        }

        .privacy-compliance a {
            color: var(--primary-blue);
            text-decoration: none;
            font-weight: 500;
        }

        .privacy-compliance a:hover {
            text-decoration: underline;
        }

        /* Terms */
        .terms-notice {
            font-size: 0.875rem;
            color: var(--text-gray);
            line-height: 1.5;
            max-width: 400px;
            margin: 0 auto;
            margin-bottom: 2rem;
        }

        .terms-notice a {
            color: var(--primary-blue);
            text-decoration: none;
            font-weight: 500;
        }

        .terms-notice a:hover {
            text-decoration: underline;
        }

        /* Footer */
        .footer {
            margin-top: 2rem;
            padding-top: 1.5rem;
            border-top: 1px solid var(--border);
            color: var(--text-light);
            font-size: 0.875rem;
        }

        /* Loading state */
        .loading {
            display: inline-block;
            width: 16px;
            height: 16px;
            border: 2px solid transparent;
            border-top: 2px solid currentColor;
            border-radius: 50%;
            animation: spin 1s linear infinite;
            margin-right: 0.5rem;
        }

        @keyframes spin {
            to { transform: rotate(360deg); }
        }

        /* Animations */
        @keyframes fadeIn {
            from { 
                opacity: 0; 
                transform: translateY(20px); 
            }
            to { 
                opacity: 1; 
                transform: translateY(0); 
            }
        }

        .fade-in {
            animation: fadeIn 0.6s ease-out;
        }

        /* Responsive */
        @media (max-width: 768px) {
            body {
                padding: 0.5rem;
                align-items: flex-start;
                padding-top: 10vh;
            }
            
            .container {
                padding: 1rem 0.5rem;
                max-width: 100%;
            }

            .logo-section {
                margin-bottom: 1.5rem;
            }

            .demo-title {
                white-space: normal;
                line-height: 1.2;
                margin-bottom: 0.75rem;
            }
            
            .demo-subtitle {
                margin-bottom: 2rem;
            }

            .btn-linkedin {
                max-width: 100%;
                margin-bottom: 1.5rem;
            }
        }

        @media (max-width: 480px) {
            body {
                padding-top: 5vh;
            }

            .container {
                padding: 0.5rem;
            }

            .logo-img {
                height: 40px;
            }

            .logo-section {
                margin-bottom: 1rem;
            }

            .demo-subtitle {
                margin-bottom: 1.5rem;
            }

            .btn-linkedin {
                padding: 0.875rem 1.5rem;
            }
        }

        @media (min-height: 800px) {
            .container {
                padding: 3rem 1rem;
            }

            .logo-section {
                margin-bottom: 3rem;
            }

            .demo-subtitle {
                margin-bottom: 3rem;
            }

            .btn-linkedin {
                margin-bottom: 2.5rem;
            }
        }
    </style>
</head>
<body>
    <div class="container fade-in">
        <!-- Logo -->
        <div class="logo-section">
            <img src="<%= typeof logoUrl !== 'undefined' ? logoUrl : 'https://www.tela.com/_vercel/image?url=%2Ftela-logo.png&w=320&q=100' %>" alt="Tela Logo" class="logo-img">
        </div>

        <!-- Demo Title -->
        <h1 class="demo-title"><%= demo.title %></h1>

        <!-- Subtitle -->
        <p class="demo-subtitle">
            Demonstração exclusiva para visitantes do C-Law Experience 2025
        </p>

        <!-- LinkedIn Button -->
        <a href="<%= authUrl %>" class="btn-linkedin" id="loginBtn">
            Acessar com Linkedin
        </a>

        <!-- Terms Notice -->
        <div class="terms-notice">
            Ao continuar, você concorda com nossos 
            <a href="https://www.tela.com/terms-of-use" target="_blank">Termos de Uso</a> e 
            <a href="https://www.tela.com/privacy" target="_blank">Política de Privacidade</a>
        </div>

        <!-- Footer -->
        <div class="footer">
            &copy; 2025 Tela. Todos os direitos reservados.
        </div>
    </div>

    <script>
        // Feedback visual para o botão de login
        document.getElementById('loginBtn').addEventListener('click', function(e) {
            const btn = e.target;
            const originalContent = btn.innerHTML;
            
            // Adicionar estado de loading
            btn.innerHTML = '<span class="loading"></span>Conectando...';
            btn.style.pointerEvents = 'none';
            btn.style.opacity = '0.8';
            
            // Restaurar estado original se a página não redirecionar
            setTimeout(() => {
                btn.innerHTML = originalContent;
                btn.style.pointerEvents = 'auto';
                btn.style.opacity = '1';
            }, 5000);
        });

        // Animação de entrada
        document.addEventListener('DOMContentLoaded', function() {
            document.querySelector('.container').classList.add('fade-in');
        });
    </script>
</body>
</html> 