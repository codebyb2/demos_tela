<!DOCTYPE html>
<html lang="pt-BR">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>C Law 2025 - Demo <%= demo.title %></title>
    
    <!-- Fonts -->
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@300;400;500;600;700;800&display=swap" rel="stylesheet">
    
    <!-- Meta tags -->
    <meta name="description" content="<%= demo.title %> - Demonstração exclusiva Tela para C-Law Experience 2025">
    <meta name="robots" content="noindex, nofollow">
    
    <style>
        :root {
            --primary-black: #000000;
            --primary-blue: #0066FF;
            --text-dark: #1a1a1a;
            --text-gray: #666666;
            --text-light: #999999;
            --background: #ffffff;
            --surface: #f8f9fa;
            --border: #e5e5e5;
            --success: #10B981;
        }

        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        body {
            font-family: 'Inter', -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, sans-serif;
            background: var(--background);
            color: var(--text-dark);
            line-height: 1.6;
            min-height: 100vh;
            padding: 1rem;
        }

        .container {
            max-width: 800px;
            margin: 0 auto;
            padding: 1.5rem 1rem;
            text-align: center;
            width: 100%;
        }

        /* Logo */
        .logo-section {
            margin-bottom: 1.5rem;
        }

        .logo-img {
            height: clamp(28px, 5.5vw, 42px);
            width: auto;
        }

        /* Demo Title */
        .demo-title {
            font-size: clamp(1.75rem, 4.5vw, 2.25rem);
            font-weight: 600;
            color: var(--text-dark);
            margin-bottom: 0.75rem;
            line-height: 1.2;
        }

        /* Subtitle */
        .demo-subtitle {
            font-size: clamp(0.875rem, 2.5vw, 1.125rem);
            color: var(--text-gray);
            margin-bottom: 2rem;
            font-weight: 400;
            max-width: 500px;
            margin-left: auto;
            margin-right: auto;
            line-height: 1.5;
        }

        /* Video Container */
        .video-container {
            background: var(--background);
            border-radius: 16px;
            box-shadow: 0 6px 12px -2px rgb(0 0 0 / 0.1), 0 2px 4px -1px rgb(0 0 0 / 0.1);
            overflow: hidden;
            margin-bottom: 2.5rem;
            border: 1px solid var(--border);
            max-width: 100%;
        }

        .video-wrapper {
            position: relative;
            width: 100%;
            height: 0;
            padding-bottom: 56.25%; /* 16:9 aspect ratio */
        }

        .video-wrapper iframe {
            position: absolute;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            border: none;
        }

        /* CTA Section */
        .cta-section {
            margin-top: 2.5rem;
            padding: 0 1rem;
        }

        .cta-title {
            font-size: clamp(1.5rem, 3.5vw, 2rem);
            font-weight: 700;
            color: var(--text-dark);
            margin-bottom: 0.75rem;
            line-height: 1.3;
        }

        .cta-description {
            font-size: clamp(0.875rem, 2.5vw, 1.125rem);
            color: var(--text-gray);
            margin-bottom: 2rem;
            line-height: 1.6;
            max-width: 480px;
            margin-left: auto;
            margin-right: auto;
        }

        /* Buttons */
        .button-group {
            display: flex;
            flex-direction: column;
            gap: 1rem;
            align-items: center;
            max-width: 320px;
            margin: 0 auto;
        }

        .btn {
            display: inline-flex;
            align-items: center;
            justify-content: center;
            padding: 1rem 2rem;
            border-radius: 12px;
            font-size: 1rem;
            font-weight: 600;
            text-decoration: none;
            cursor: pointer;
            transition: all 0.2s ease;
            width: 100%;
            border: 2px solid transparent;
            min-height: 54px;
        }

        .btn:hover {
            transform: translateY(-2px);
            box-shadow: 0 8px 16px -4px rgb(0 0 0 / 0.2);
        }

        .btn-primary {
            background: var(--primary-black);
            color: white;
            border-color: var(--primary-black);
        }

        .btn-primary:hover {
            background: #333333;
            border-color: #333333;
            color: white;
        }

        .btn-secondary {
            background: transparent;
            color: var(--text-dark);
            border-color: var(--border);
        }

        .btn-secondary:hover {
            background: var(--surface);
            color: var(--text-dark);
            border-color: var(--text-gray);
        }

        /* Footer */
        .footer {
            margin-top: 3rem;
            padding-top: 2rem;
            border-top: 1px solid var(--border);
            color: var(--text-light);
            font-size: 0.875rem;
        }

        /* Animations */
        @keyframes fadeIn {
            from { 
                opacity: 0; 
                transform: translateY(20px); 
            }
            to { 
                opacity: 1; 
                transform: translateY(0); 
            }
        }

        .fade-in {
            animation: fadeIn 0.6s ease-out;
        }

        /* Loading state */
        .video-loading {
            position: absolute;
            top: 50%;
            left: 50%;
            transform: translate(-50%, -50%);
            color: var(--text-gray);
            font-size: 1rem;
        }

        /* Responsive */
        @media (max-width: 768px) {
            body {
                padding: 0.5rem;
            }

            .container {
                padding: 1rem 0.5rem;
                max-width: 100%;
            }

            .logo-section {
                margin-bottom: 1.5rem;
            }
            
            .demo-subtitle {
                margin-bottom: 2rem;
            }

            .video-container {
                border-radius: 16px;
                margin-bottom: 2rem;
            }

            .cta-section {
                margin-top: 2rem;
                padding: 0 0.5rem;
            }

            .cta-description {
                margin-bottom: 2rem;
            }

            .button-group {
                max-width: 100%;
                gap: 0.75rem;
            }

            .footer {
                margin-top: 2rem;
            }
        }

        @media (max-width: 480px) {
            body {
                padding: 0.25rem;
            }

            .container {
                padding: 0.5rem 0.25rem;
            }

            .logo-img {
                height: 40px;
            }

            .logo-section {
                margin-bottom: 1rem;
            }

            .demo-subtitle {
                margin-bottom: 1.5rem;
            }

            .video-container {
                border-radius: 12px;
                margin-bottom: 1.5rem;
            }

            .cta-section {
                margin-top: 1.5rem;
                padding: 0 0.25rem;
            }

            .btn {
                padding: 0.875rem 1.5rem;
                font-size: 0.95rem;
            }
        }

        @media (min-width: 769px) {
            .container {
                padding: 3rem 1rem;
            }

            .logo-section {
                margin-bottom: 3rem;
            }

            .demo-subtitle {
                margin-bottom: 3rem;
            }

            .video-container {
                margin-bottom: 4rem;
            }

            .cta-section {
                margin-top: 4rem;
            }

            .button-group {
                flex-direction: row;
                justify-content: center;
                max-width: 600px;
            }

            .btn {
                min-width: 200px;
                max-width: 280px;
            }
        }

        @media (min-width: 1200px) {
            .container {
                max-width: 1000px;
            }

            .video-container {
                border-radius: 24px;
            }
        }
    </style>
</head>
<body>
    <div class="container">
        <!-- 1. Logo centralizado no topo -->
        <div class="logo-section fade-in">
            <img src="<%= typeof logoUrl !== 'undefined' ? logoUrl : 'https://www.tela.com/_vercel/image?url=%2Ftela-logo.png&w=320&q=100' %>" alt="Tela Logo" class="logo-img">
        </div>

        <!-- 2. Nome da demo Tela -->
        <h1 class="demo-title fade-in">
            <%= demo.title %>
        </h1>

        <!-- 3. Demonstração exclusiva para visitantes do C-Law Experience 2025 -->
        <p class="demo-subtitle fade-in">
            Demonstração exclusiva para visitantes do C-Law Experience 2025
        </p>

        <!-- 4. Embed do vídeo -->
        <div class="video-container fade-in">
            <div class="video-wrapper">
                <iframe 
                    src="https://www.youtube.com/embed/<%= youtubeId %>?autoplay=1&rel=0&showinfo=0&modestbranding=1"
                    allow="accelerometer; autoplay; clipboard-write; encrypted-media; gyroscope; picture-in-picture; web-share"
                    allowfullscreen
                    title="<%= demo.title %>"
                    loading="lazy">
                </iframe>
            </div>
        </div>

        <!-- 5. CTA Section -->
        <div class="cta-section fade-in">
            <h2 class="cta-title">
                Pronto para revolucionar seu escritório?
            </h2>
            
            <p class="cta-description">
                Descubra como o Tela pode otimizar seus processos e aumentar a eficiência da sua equipe jurídica.
            </p>
            
            <div class="button-group">
                <a href="https://cal.com/team/tela/demo" 
                   class="btn btn-primary" 
                   target="_blank"
                   rel="noopener noreferrer">
                    Agendar Reunião
                </a>
                <a href="/demos" 
                   class="btn btn-secondary">
                    Outras Demos
                </a>
                <a href="https://tela.com/" 
                   class="btn btn-secondary" 
                   target="_blank"
                   rel="noopener noreferrer">
                    Tela.com
                </a>
            </div>
        </div>

        <!-- Footer -->
        <div class="footer">
            &copy; 2025 Tela. Todos os direitos reservados.
        </div>
    </div>

    <script>
        // Add staggered animation delays
        document.addEventListener('DOMContentLoaded', function() {
            const elements = document.querySelectorAll('.fade-in');
            elements.forEach((element, index) => {
                element.style.animationDelay = `${index * 0.1}s`;
            });
        });
    </script>
</body>
</html> 